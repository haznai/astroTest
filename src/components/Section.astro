---
export interface Props {
    header: string;
}

const { header } = Astro.props as Props;
---

<!-- <section
    class="bg-[url('/assets/bg1.png')] bg-[length:30%] bg-[right_4rem_bottom_3rem] bg-no-repeat h-96"
>
    <div class="backdrop-filter backdrop blur-3xl h-full">
        <h1 class="font-sans text-2xl md:text-4xl pl-4 md:pl-6">
            {header}
            <slot />
        </h1>
    </div>
</section> -->
<section>
    <div class="absolute max-w-sm mx-auto w-full rounded-md">
        <div
            class="absolute -left-4 w-72 h-72 bg-blue-400 rounded-full filter blur-3xl opacity-20 animate-blob"
        >
        </div>
        <div
            class="absolute -right-4 w-72 h-72 bg-yellow-300 rounded-full filter blur-3xl opacity-70 animate-blob"
        >
        </div>
        <div
            class="absolute -bottom-8 left-20 w-72 h-72 bg-pink-300 rounded-full filter blur-3xl opacity-70 animate-blob"
        >
        </div>
    </div>
</section>
